#language: generic
sudo: required
services:
    - docker

before_install:
    - docker build -t aseeni/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true aseeni/docker-react npm run test
#script:
# - docker run aseeni/docker-react npm run test -- --coverage
#script:
# - docker run -e CI=true aseeni/docker-react npm run test
